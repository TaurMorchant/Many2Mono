# Поддержка прошлых LTS веток в монорепе

## Проблема

При объединении нескольких репозиториев в монорепу возникает вопрос: как обеспечить поддержку прошлых LTS веток исходных репозиториев?

### Что нужно обеспечить

1. Возможность выпускать патчи для старых LTS версий
2. Согласованность версий зависимых модулей в рамках одной LTS
3. Минимизация сложности поддержки
4. Сохранение git-истории для LTS веток

---

## Контекст

### Текущая ситуация

- Каждый исходный репозиторий имеет свои LTS ветки с именованием `lts/25.3`, `lts/25.4` и т.д.
- Каждый исходный репозиторий версионируется **независимо**
- Git-теги ставятся на каждую maven-версию модуля (не релизные теги, а теги версий)
- Модули **зависят друг от друга** — при патче одного модуля нужно обновлять зависимые
- За согласование версий отвечает **релизная тула** (самописная), которая:
  - Принимает список репозиториев для совместного релиза
  - Строит граф зависимостей
  - Повышает версии и обновляет зависимости
  - Делает релиз каждого репозитория
- **Ограничения релизной тулы:**
  - Не умеет отводить LTS ветки (делается вручную)
  - Не умеет работать с монорепой (только с отдельными репозиториями)

### Политика LTS

- 4 квартальных LTS в год (25.1, 25.2, 25.3, 25.4)
- Каждая LTS поддерживается 2 года
- В LTS ветках — только багфиксы

### Релизный процесс

- **Main ветка:** разработка → релиз изменённых модулей раз в спринт (на практике меняются все)
- **Конец квартала:** отводим LTS ветку от main
- **LTS ветка:** багфиксы по запросу

При любом релизе (спринтовый или багфикс):
1. Повысить версию изменённого модуля
2. Обновить версии в зависимых модулях (которые используют изменённый)
3. Это делает релизная тула

### Новые LTS (после миграции в монорепу)

Для новых LTS (26.2 и далее) процесс **однозначен**:

1. **Отведение LTS:** в конце квартала создаём ветку `lts/26.2` от main — обычная git-ветка, содержит все модули
2. **Багфиксы:** делаем в ветке `lts/26.2`, релизная тула обновляет версии изменённых и зависимых модулей
3. **Структура:** единая ветка со всеми модулями, как main

**Требуется:** адаптация релизной тулы для работы с монорепой (модули в одном репозитории вместо разных репозиториев).

**Вывод:** Для новых LTS проблем нет. Варианты решения (ниже) относятся только к **поддержке прошлых LTS** (25.3, 25.4 и т.д.), которые были отведены ещё в исходных репозиториях до миграции.

### Как работает Many2Mono сейчас

- Клонирует **все ветки** из параметра `BRANCHES` (по умолчанию только `main`)
- Применяет `git filter-repo` к каждой ветке отдельно
- Мержит истории с `--allow-unrelated-histories`
- **Поддержка LTS веток:** Реализована через параметр `BRANCHES` в `config.env`
  - Пример: `BRANCHES=main lts/25.3 lts/25.4`
  - Все указанные ветки переносятся в монорепу

---

## Визуализация

### Исходное состояние: два отдельных репозитория

**Репозиторий A (например, core-utils):**
```
$ git log --graph --pretty=format:"%s%d" --all

* A5  (HEAD -> main)
* A4
| * A-lts-1  (lts/25.4)
|/
* A3
* A2
* A1
```

**Репозиторий B (например, core-error-handling):**
```
$ git log --graph --pretty=format:"%s%d" --all

* B5  (HEAD -> main)
* B4
| * B-lts-1  (lts/25.4)
|/
* B3
* B2
* B1
```

### Текущее состояние: монорепа после Many2Mono (только main)

Это то, что получается сейчас при запуске `make all` без указания BRANCHES:

```
$ git log --graph --pretty=format:"%s%d" --all

*   Merge B  (HEAD -> main)
|\
| * B5
| * B4
| * B3
| * B2
| * B1
*   Merge A
|\
| * A5
| * A4
| * A3
| * A2
| * A1
* Init
```

**Проблема (до реализации):** LTS ветки (`lts/25.4`) из исходных репозиториев **не перенесены** в монорепу.

---

### Результат после реализации: монорепа с LTS поддержкой

После реализации поддержки LTS веток, при запуске с `BRANCHES="main test/vlla"`:

```
$ git log --graph --pretty=format:"%s%d" --all

*   Merge core-error-handling (test/vlla)  (HEAD -> test/vlla)
|\
| * test commit (B)
* |   Merge core-utils (test/vlla)
|\ \
| * | test commit (A)
* | | Initial monorepo commit (test/vlla)
 / /
| | *   Merge core-error-handling (main)  (main)
| | |\
| | | * B5
| | |/
| |/|
| * | B4
| * | B3
| * | B2
| * | B1
|  /
| *   Merge core-utils (main)
| |\
| | * A5
| |/
|/|
* | A4
* | A3
* | A2
* | A1
 /
* Initial monorepo commit (main)
```

---

## Варианты решения

### Вариант 1: Полный перенос LTS веток (связанные истории)

**Идея:** LTS ветка собирается **отдельно** тем же процессом clone+filter-repo+merge. Создаётся как orphan branch, но при мерже репозиториев сохраняется их общая история.

**Git-дерево (упрощённо, на основе реальных репозиториев A и B):**

```
$ git log --graph --pretty=format:"%s%d" --all

*   Merge B (lts/25.4)  (HEAD -> lts/25.4)
|\
| * B-lts-1
* |   Merge A (lts/25.4)
|\ \
| * | A-lts-1
* | | Initial monorepo commit (lts/25.4)
 / /
| | *   Merge B (main)  (main)
| | |\
| | | * B5
| | |/
| |/|
| * | B4
| * | B3
| * | B2
| * | B1
|  /
| *   Merge A (main)
| |\
| | * A5
| |/
|/|
* | A4
* | A3
* | A2
* | A1
 /
* Initial monorepo commit (main)
```

**Важно:** Ветки `main` и `lts/25.4` создаются независимо (orphan branches с разными "Initial monorepo commit"), но **имеют общего предка** через коммиты из исходных репозиториев. Коммиты A1-A4, B1-B4 — это **одни и те же коммиты** с одинаковыми SHA, присутствующие в обеих ветках монорепы.

**Как создаётся:**
1. Для первой ветки (main):
   - `git init -b main` — создаём новую монорепу
   - Клонируем все репозитории с веткой `main`
   - `git filter-repo --to-subdirectory-filter` — перемещаем в субдиректории
   - `git merge --allow-unrelated-histories` — объединяем истории
2. Для остальных веток (lts/25.4):
   - `git checkout --orphan lts/25.4` — создаём orphan branch
   - Клонируем все репозитории с веткой `lts/25.4`
   - `git filter-repo --to-subdirectory-filter` — перемещаем в субдиректории
   - `git merge --allow-unrelated-histories` — объединяем истории
3. Результат: Ветки технически независимы (разные Init коммиты), но **связаны через общую историю** исходных репозиториев

**Анализ:**

Плюсы:
- ✅ Единая LTS ветка — простой `git checkout lts/25.4`
- ✅ Простой релизный процесс (всё в одной ветке)
- ✅ Низкая сложность CI/CD
- ✅ **Общая история** — коммиты до точки ответвления LTS одинаковые (одинаковые SHA)
- ✅ **Git инструменты работают корректно:**
  - `git log main..lts/25.4` — показывает отличия
  - `git diff main lts/25.4` — работает с контекстом
  - `git cherry-pick` — проще, есть общий контекст
- ✅ **Нет дублирования истории** — коммиты до точки ответвления существуют в единственном экземпляре

Подводные камни:
1. **Визуализация графа** — `git log --all --graph` показывает переплетённые ветки, может сбивать с толку новичков
2. **Orphan branches** — технически ветки не связаны напрямую через родительские коммиты, но связаны через merge коммиты исходных репозиториев

**Вывод:** Оптимальное решение. Сохраняет простоту эксплуатации (единая ветка) и преимущества общей истории (git инструменты, нет дублирования).

---

### Вариант 2: LTS ветки для каждого модуля в общем дереве

**Идея:** LTS ветки переносятся как часть общего дерева. Каждая LTS ветка отводится от "своего" коммита (A3 или B3) и содержит только файлы своего модуля.

**Git-дерево:**
```
$ git log --graph --pretty=format:"%s%d" --all

*   Merge B  (HEAD -> main)
|\
| * B5
| * B4
| | * B-lts-1  (lts/25.4/core-error-handling)
| |/
| * B3
| * B2
| * B1
*   Merge A
|\
| * A5
| * A4
| | * A-lts-1  (lts/25.4/core-utils)
| |/
| * A3
| * A2
| * A1
* Init
```

**Структура веток:**
- `main` — объединённая монорепа (core-utils/ + core-error-handling/)
- `lts/25.4/core-utils` — отведена от A3, содержит только файлы в core-utils/
- `lts/25.4/core-error-handling` — отведена от B3, содержит только файлы в core-error-handling/

**Как создаётся:**
1. Объединяем main ветки (как сейчас)
2. При клонировании каждого репозитория также клонируем его LTS ветки
3. После filter-repo LTS ветки остаются в дереве, отведённые от нужных коммитов
4. Имена веток переименовываются: `lts/25.4` → `lts/25.4/<module-name>`

**Как собирать релиз LTS 25.4?**

Нужен механизм, который:
1. Берёт `core-utils/` из ветки `lts/25.4/core-utils`
2. Берёт `core-error-handling/` из ветки `lts/25.4/core-error-handling`
3. Собирает их вместе для билда/релиза

Возможные реализации:
```bash
# CI/CD скрипт:
git checkout main  # или любая ветка с полной структурой
git checkout lts/25.4/core-utils -- core-utils/
git checkout lts/25.4/core-error-handling -- core-error-handling/
mvn deploy
```

**Анализ:**

Плюсы:
- LTS ветки — часть общего дерева (есть общий предок с main)
- `git log main..lts/25.4/core-utils` работает корректно
- cherry-pick между ветками проще (общая история)
- Нет дублирования коммитов A1-A3, B1-B3

Проблемы:
1. **Нет единой ветки LTS** — нельзя просто `git checkout lts/25.4` и получить всё
2. **Сложность CI/CD** — нужна специальная логика сборки
3. **Неполные ветки** — `git checkout lts/25.4/core-utils` даёт только core-utils/, остальных директорий нет
4. **Сложность релизного процесса** — при багфиксе модуля A нужно обновить зависимости в модуле C, но это разные ветки (`lts/25.4/core-utils` и `lts/25.4/core-error-handling`). Два коммита в двух ветках — как гарантировать атомарность? Как релизная тула будет с этим работать?
5. **Когерентность** — как гарантировать, что версии в разных ветках совместимы?

**Вывод:** Сохраняет связь с основным деревом, но усложняет релизный процесс и теряет единую точку сборки для LTS.

---

## Сравнение вариантов

| Аспект | Вариант 1 | Вариант 2 |
|--------|-----------|-----------|
| Единая LTS ветка | ✅ Да | ❌ Нет (по модулям) |
| Связь main и lts | ✅ Да (через общую историю) | ✅ Да |
| Простота checkout | ✅ Простой | ❌ Сложный |
| Сложность CI/CD | ✅ Низкая | ❌ Высокая |
| Сложность релиза в LTS | ✅ Низкая (одна ветка) | ❌ Высокая (координация веток) |
| Размер репо | ✅ Нормальный (нет дублей) | ✅ Нормальный |
| Теги версий | На модуль в единой ветке | На модуль в своей ветке |
| cherry-pick между ветками | ✅ Работает (общий контекст) | ✅ Работает |
| git log / git diff | ✅ Работает корректно | ✅ Работает корректно |

---

## Рассмотренные и отброшенные альтернативы

### Альтернатива A: Единая LTS ветка, отведённая от main монорепы

**Идея:** LTS ветка создаётся **после** объединения, как обычная ветка в монорепе.

**Почему не работает:**

```
*   Merge B  (HEAD -> main)
|\
| * B5
| * B4
| * B3    <-- если отвести LTS отсюда, нет файлов из A!
| * B2
| * B1
*   Merge A
|\
| * A5
| * A4
| * A3    <-- если отвести отсюда, нет файлов из B!
| * A2
| * A1
* Init    <-- пустой коммит, нет ничего
```

В монорепе **нет коммита**, где одновременно:
- Репо A в состоянии A3 (точка отведения LTS)
- Репо B в состоянии B3 (точка отведения LTS)

Единственный коммит с файлами из обоих репо — `Merge B`, но там уже A5 и B5.

**Вывод:** Невозможен в текущий условиях.

---

### Альтернатива B: LTS остаются в исходных репозиториях

**Идея:** Монорепа содержит только main, LTS поддержка продолжается в исходных репозиториях.

**Минусы:**
- Два места для поддержки кода (монорепа + исходные репо)
- Сложнее синхронизировать багфиксы
- Нарушает идею "единого источника правды"
- Со временем исходные репо устаревают и путаются

**Вывод:** Анти-паттерн для монорепы. **Не подходит** — цель отказаться от старых репо полностью.

---

### Альтернатива C: Гибридный подход — поэтапная миграция

**Идея:** Новые LTS создаются в монорепе, старые LTS остаются в исходных репозиториях до истечения срока поддержки.

**Минусы:**
- Временно поддерживаем два места
- Нужно чётко документировать, где какая LTS

**Вывод:** **Не подходит** — цель отказаться от старых репо полностью.

---

## Открытые вопросы

1. **Синхронизация LTS:** Все ли репозитории имеют одинаковые LTS ветки (lts/25.3, lts/25.4)?
   - Если нет — что делать с репозиторием, у которого нет ветки?

2. **Релизная тула:** Какие изменения нужны для работы с монорепой?
   - Работа с модулями вместо репозиториев
   - Создание LTS веток

3. **Согласованность версий:** Как обеспечить, что в LTS ветке монорепы все модули совместимы?

4. **CI/CD:** Как будет выглядеть CI для LTS веток монорепы?

---

## Рекомендуемое решение

**Выбран Вариант 1: Полный перенос LTS веток (связанные истории)**

### Обоснование

Учитывая следующие факторы:
- **Временность проблемы:** Прошлые LTS (25.3, 25.4) живут максимум 2 года, после чего отомрут естественным образом
- **Критичность релизного процесса:** Багфиксы для клиентов требуют надёжного и простого процесса
- **Простота эксплуатации:** Единая LTS ветка упрощает работу команды (checkout, CI/CD, релиз)
- **Сохранение git-истории:** Ветки связаны через общую историю исходных репозиториев

Вариант 1 **не имеет значимых недостатков** — он обеспечивает и простоту использования, и корректную работу git инструментов.

### Что получаем

1. **Единая LTS ветка** для каждой версии (`lts/25.3`, `lts/25.4`)
2. **Простой checkout:** `git checkout lts/25.4` — и всё готово к работе
3. **Простой релизный процесс:** релизная тула работает как обычно (одна ветка, все модули внутри)
4. **Низкая сложность CI/CD:** стандартные пайплайны без специальной логики
5. **Общая история:** Ветки связаны через коммиты исходных репозиториев
6. **Git инструменты работают:** `git log`, `git diff`, `git cherry-pick` корректно обрабатывают связь между ветками
7. **Нет дублирования:** Коммиты до точки ответвления LTS существуют в единственном экземпляре

### Минимальные компромиссы

1. **Визуализация графа:** `git log --all --graph` показывает переплетённые ветки (но это отражает реальность)
2. **Orphan branches:** Технически ветки не связаны через parent коммиты (но связаны через merge коммиты)

### Вывод

Вариант 1 является **оптимальным решением**, обеспечивая простоту эксплуатации без существенных технических недостатков. Ветки автоматически получают общую историю благодаря тому, что исходные репозитории отводили LTS от main.

---

## План реализации

### Этап 1: Расширение Makefile для поддержки нескольких веток ✅ ВЫПОЛНЕНО

**Цель:** Модифицировать Many2Mono для клонирования и объединения нескольких веток.

**Реализовано:**

1. **Добавлен параметр `BRANCHES` в `config.env`** (по умолчанию `main`):
   ```bash
   # config.env.template
   #BRANCHES=main
   ```
   Следуя архитектуре проекта, параметр вынесен в конфигурацию, а не в Makefile.

2. **Модифицирован `clone` target** с добавлением:
   - Проверки существования ветки в удалённом репозитории (если ветка отсутствует — **ошибка и прерывание**)
   - Поддержки веток с `/` в имени (через замену на `-` для файловой системы)
   - Клонирования каждой ветки отдельно с применением `filter-repo`

   Ключевые изменения:
   ```bash
   for branch in $(BRANCHES); do
     branch_safe="$$(echo "$$branch" | tr '/' '-')"
     bare="$(TMP_DIR)/$${repo}-$${branch_safe}.git"

     # Проверка существования ветки
     if ! git ls-remote --heads "$$url" "$$branch" | grep -q "$$branch"; then
       echo "[ERROR] Branch $$branch not found in $$repo" >&2
       exit 1
     fi

     git clone --bare --branch "$$branch" --single-branch "$$url" "$$bare"
     # ... filter-repo
   done
   ```

3. **Модифицирован `merge` target** с добавлением:
   - Создания orphan branch для не-main веток
   - **Очистки рабочей директории** после создания orphan branch (критично для избежания конфликтов)
   - Поддержки безопасных имён веток

   Ключевые изменения:
   ```bash
   for branch in $(BRANCHES); do
     if [ "$$first_branch" = true ]; then
       rm -rf "$(MONOREPO_DIR)"
       git init -b "$$branch" "$(MONOREPO_DIR)"
       first_branch=false
     else
       # Создаём orphan branch и очищаем рабочую директорию
       (
         cd "$(MONOREPO_DIR)"
         git checkout --orphan "$$branch"
         git rm -rf . 2>/dev/null || true  # КРИТИЧНО!
       )
     fi
     # ... merge logic
   done
   ```

**Важное уточнение:**
- Ветки создаются как orphan, но **получают общую историю** через merge коммитов исходных репозиториев
- `git rm -rf .` после `checkout --orphan` критичен для избежания add/add конфликтов

### Этап 2: Тестирование на тестовых репозиториях ✅ ВЫПОЛНЕНО

**Выполнено:**
1. Созданы тестовые ветки `test/vlla` в репозиториях
2. Запущен процесс с `BRANCHES="main test/vlla"` в `config.env`
3. Результат успешно проверен:
   - `git log --all --graph` показывает связанные ветки через общую историю
   - `git checkout main` — все модули в актуальном состоянии
   - `git checkout test/vlla` — все модули в состоянии тестовой ветки
   - `git merge-base main test/vlla` — возвращает общего предка (история связана ✅)

**Вывод:** Механизм работает корректно. Ветки связаны через общую историю исходных репозиториев, как и ожидалось.

### Этап 3: Миграция реальных репозиториев

**Подготовка:**
1. Определить список активных LTS веток (например: `lts/25.3`, `lts/25.4`)
2. ⚠️ **ВАЖНО:** Убедиться, что **все** репозитории из `repos.txt` имеют **все** указанные ветки
   - Инструмент падает с ошибкой, если хотя бы одна ветка отсутствует в каком-либо репозитории
3. Настроить `config.env`:
   ```bash
   # config.env
   BRANCHES=main lts/25.3 lts/25.4
   ```

**Запуск:**
```bash
make all
```

**Проверка:**
1. Проверить размер репозитория: `du -sh monorepo/`
2. Проверить структуру веток: `cd monorepo && git branch -a`
3. Проверить историю: `git log --all --graph --oneline`
4. Проверить наличие общего предка:
   ```bash
   git merge-base main lts/25.3
   # Должен вернуть SHA коммита (не ошибку)
   ```
5. Проверить файлы в каждой ветке:
   ```bash
   git checkout main && ls -la
   git checkout lts/25.3 && ls -la
   ```

### Этап 4: Адаптация релизной тулы

**Требования:**
1. Работа с модулями в одном репозитории вместо разных репозиториев
2. Поддержка нескольких веток (main, lts/*)
3. Построение графа зависимостей внутри монорепы
4. Обновление версий в pom.xml модулей

**Детали:** Требует отдельного дизайн-документа для релизной тулы.

### Этап 5: Настройка CI/CD

**Для каждой LTS ветки:**
1. Создать отдельный workflow/pipeline
2. Триггеры: push в `lts/*` ветки
3. Сборка и тесты всех модулей
4. Публикация артефактов

**Пример GitHub Actions:**
```yaml
name: LTS 25.4 CI

on:
  push:
    branches:
      - lts/25.4

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
      - name: Build with Maven
        run: mvn clean install
```

### Этап 6: Документирование

1. Обновить README монорепы с информацией о LTS ветках
2. Задокументировать процесс работы с LTS (checkout, багфиксы, релиз)
3. Обучить команду новому процессу

### Открытые задачи

1. **Обработка отсутствующих LTS веток:** ~~Что делать, если репозиторий не имеет какой-то LTS ветки?~~
   - ✅ **Решено:** Скрипт падает с ошибкой. Все репозитории должны иметь все указанные ветки.

2. **Размер репозитория:** ~~Оценить увеличение размера после переноса всех LTS~~
   - ✅ **Решено:** Коммиты до точки ответвления LTS **не дублируются** (одинаковые SHA), поэтому значительного увеличения размера не происходит
   - Минимальное увеличение связано только с новыми коммитами в LTS ветках (багфиксы)

3. **Миграция тегов:** Нужно ли переносить git-теги из исходных репозиториев?
   - **Статус:** Требуется решение
   - **Вопрос:** Переносить ли теги версий (например, `3.4.0`, `3.3.0`) в монорепу?
   - **Варианты:**
     - Не переносить — теги остаются в исходных репозиториях (для истории)
     - Переносить с префиксом — например, `core-utils/3.4.0`
     - Переносить как есть — может привести к конфликтам имён
